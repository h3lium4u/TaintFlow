[package]
name = "rules"
version = "0.1.0"
edition = "2021"

[dependencies]
parser = { path = "../parser" }
normalizer = { path = "../normalizer" }
symbols = { path = "../symbols" }
cfg = { path = "../cfg" }
taint = { path = "../taint" }
serde = { version = "1.0", features = ["derive"] }
serde_yaml = "0.9"
serde_json = "1.0"
