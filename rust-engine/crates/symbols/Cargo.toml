[package]
name = "symbols"
version = "0.1.0"
edition = "2021"

[dependencies]
parser = { path = "../parser" }
ir = { path = "../ir" }
normalizer = { path = "../normalizer" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

